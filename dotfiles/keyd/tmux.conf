# Caps Lock emits the terminal modifiers used by the shared tmux key map.
# tmux calls Alt "M"; keyd calls it "A". Super is deliberately not included.
[ids]
*

[main]
capslock = layer(tmux)

[tmux:C-A-S]
